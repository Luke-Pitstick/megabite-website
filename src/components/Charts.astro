---
// Chart component library for Megabite
---

<!-- ROI Breakdown Chart -->
<div id="roi-chart" class="w-full h-64 bg-white rounded-lg p-4"></div>

<!-- Cost Comparison Chart -->
<div id="cost-comparison" class="w-full h-64 bg-white rounded-lg p-4"></div>

<!-- Implementation Timeline -->
<div class="implementation-timeline">
  <div class="flex items-center justify-between mb-8">
    <div class="flex-1 text-center">
      <div class="w-12 h-12 bg-pink-500 rounded-full mx-auto mb-2 flex items-center justify-center">
        <span class="text-white font-bold">1</span>
      </div>
      <h4 class="font-semibold text-gray-900">Audit</h4>
      <p class="text-sm text-gray-600">Week 1-2</p>
    </div>
    <div class="flex-1 text-center">
      <div class="w-12 h-12 bg-pink-500 rounded-full mx-auto mb-2 flex items-center justify-center">
        <span class="text-white font-bold">2</span>
      </div>
      <h4 class="font-semibold text-gray-900">Implementation</h4>
      <p class="text-sm text-gray-600">Week 3-8</p>
    </div>
    <div class="flex-1 text-center">
      <div class="w-12 h-12 bg-pink-500 rounded-full mx-auto mb-2 flex items-center justify-center">
        <span class="text-white font-bold">3</span>
      </div>
      <h4 class="font-semibold text-gray-900">Optimization</h4>
      <p class="text-sm text-gray-600">Ongoing</p>
    </div>
  </div>
</div>

<!-- Progress Bars for Metrics -->
<div class="metric-bars space-y-4">
  <div>
    <div class="flex justify-between mb-1">
      <span class="text-sm font-medium text-gray-700">Labor Cost Reduction</span>
      <span class="text-sm text-gray-500">15%</span>
    </div>
    <div class="w-full bg-gray-200 rounded-full h-2">
      <div class="bg-green-500 h-2 rounded-full" style="width: 15%"></div>
    </div>
  </div>
  <div>
    <div class="flex justify-between mb-1">
      <span class="text-sm font-medium text-gray-700">Food Cost Savings</span>
      <span class="text-sm text-gray-500">8%</span>
    </div>
    <div class="w-full bg-gray-200 rounded-full h-2">
      <div class="bg-blue-500 h-2 rounded-full" style="width: 8%"></div>
    </div>
  </div>
  <div>
    <div class="flex justify-between mb-1">
      <span class="text-sm font-medium text-gray-700">Revenue Increase</span>
      <span class="text-sm text-gray-500">12%</span>
    </div>
    <div class="w-full bg-gray-200 rounded-full h-2">
      <div class="bg-purple-500 h-2 rounded-full" style="width: 12%"></div>
    </div>
  </div>
</div>

<!-- Before/After Comparison Cards -->
<div class="before-after-grid grid md:grid-cols-2 gap-6">
  <div class="bg-red-50 border border-red-200 p-6 rounded-xl">
    <h4 class="font-bold text-red-800 mb-4">Before: Disconnected Systems</h4>
    <ul class="space-y-2 text-red-700">
      <li class="flex items-start">
        <svg class="w-5 h-5 text-red-500 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
        Manual inventory tracking
      </li>
      <li class="flex items-start">
        <svg class="w-5 h-5 text-red-500 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
        No real-time data visibility
      </li>
      <li class="flex items-start">
        <svg class="w-5 h-5 text-red-500 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
        Multiple disconnected vendors
      </li>
      <li class="flex items-start">
        <svg class="w-5 h-5 text-red-500 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
        Reactive problem solving
      </li>
    </ul>
  </div>
  <div class="bg-green-50 border border-green-200 p-6 rounded-xl">
    <h4 class="font-bold text-green-800 mb-4">After: Unified Platform</h4>
    <ul class="space-y-2 text-green-700">
      <li class="flex items-start">
        <svg class="w-5 h-5 text-green-500 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
        Real-time inventory tracking
      </li>
      <li class="flex items-start">
        <svg class="w-5 h-5 text-green-500 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
        Unified dashboard visibility
      </li>
      <li class="flex items-start">
        <svg class="w-5 h-5 text-green-500 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
        Single point of contact
      </li>
      <li class="flex items-start">
        <svg class="w-5 h-5 text-green-500 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
        Proactive optimization
      </li>
    </ul>
  </div>
</div>

<!-- Animated Counter Stats -->
<div class="stats-grid grid grid-cols-2 md:grid-cols-4 gap-6">
  <div class="text-center">
    <div class="text-4xl font-bold text-pink-600 counter" data-target="142600">$0</div>
    <p class="text-gray-600 mt-2">Average ROI</p>
  </div>
  <div class="text-center">
    <div class="text-4xl font-bold text-pink-600 counter" data-target="8">0</div>
    <p class="text-gray-600 mt-2">Food Cost ROI</p>
  </div>
  <div class="text-center">
    <div class="text-4xl font-bold text-pink-600 counter" data-target="99">0%</div>
    <p class="text-gray-600 mt-2">Labor Cost Increase</p>
  </div>
  <div class="text-center">
    <div class="text-4xl font-bold text-pink-600 counter" data-target="3">0</div>
    <p class="text-gray-600 mt-2">Month Payback</p>
  </div>
</div>

<!-- Integration Flow Diagram -->
<div class="integration-diagram bg-white p-8 rounded-xl border border-pink-100">
  <h4 class="text-xl font-bold text-center mb-8">How Your Systems Work Together</h4>
  <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
    <!-- POS Central -->
    <div class="text-center">
      <div class="w-24 h-24 bg-pink-500 rounded-full mx-auto mb-4 flex items-center justify-center">
        <span class="text-white font-bold">POS</span>
      </div>
      <p class="text-sm text-gray-600">Central Hub</p>
    </div>
    
    <!-- Connected Systems -->
    <div class="grid grid-cols-2 gap-4">
      <div class="text-center">
        <div class="w-16 h-16 bg-blue-500 rounded-full mx-auto mb-2 flex items-center justify-center">
          <span class="text-white text-xs">INV</span>
        </div>
        <p class="text-xs text-gray-600">Inventory</p>
      </div>
      <div class="text-center">
        <div class="w-16 h-16 bg-green-500 rounded-full mx-auto mb-2 flex items-center justify-center">
          <span class="text-white text-xs">LAB</span>
        </div>
        <p class="text-xs text-gray-600">Labor</p>
      </div>
      <div class="text-center">
        <div class="w-16 h-16 bg-purple-500 rounded-full mx-auto mb-2 flex items-center justify-center">
          <span class="text-white text-xs">CRM</span>
        </div>
        <p class="text-xs text-gray-600">Guest Data</p>
      </div>
      <div class="text-center">
        <div class="w-16 h-16 bg-orange-500 rounded-full mx-auto mb-2 flex items-center justify-center">
          <span class="text-white text-xs">ACC</span>
        </div>
        <p class="text-xs text-gray-600">Accounting</p>
      </div>
    </div>
    
    <!-- Results -->
    <div class="text-center">
      <div class="w-24 h-24 bg-gradient-to-r from-pink-500 to-pink-600 rounded-full mx-auto mb-4 flex items-center justify-center">
        <span class="text-white font-bold">ROI</span>
      </div>
      <p class="text-sm text-gray-600">Profit Growth</p>
    </div>
  </div>
</div>

<script>
  // Animated Counter Function
  function animateCounter() {
    const counters = document.querySelectorAll('.counter');
    
    counters.forEach(counter => {
      const target = parseInt(counter.getAttribute('data-target'));
      const increment = target / 100;
      let current = 0;
      
      const updateCounter = () => {
        current += increment;
        if (current < target) {
          if (counter.getAttribute('data-target') === '142600') {
            counter.textContent = '$' + Math.ceil(current).toLocaleString();
          } else if (counter.getAttribute('data-target') === '99') {
            counter.textContent = Math.ceil(current) + '%';
          } else {
            counter.textContent = Math.ceil(current).toString();
          }
          requestAnimationFrame(updateCounter);
        } else {
          if (counter.getAttribute('data-target') === '142600') {
            counter.textContent = '$' + target.toLocaleString();
          } else if (counter.getAttribute('data-target') === '99') {
            counter.textContent = target + '%';
          } else {
            counter.textContent = target;
          }
        }
      };
      
      updateCounter();
    });
  }
  
  // Start animation when page loads
  document.addEventListener('DOMContentLoaded', animateCounter);
</script>

<style>
  .counter {
    display: inline-block;
  }
  
  .implementation-timeline {
    position: relative;
  }
  
  .implementation-timeline::before {
    content: '';
    position: absolute;
    top: 24px;
    left: 16.66%;
    right: 16.66%;
    height: 2px;
    background: linear-gradient(to right, #ec4899, #ec4899);
    z-index: 0;
  }
  
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .stats-grid > div {
    animation: fadeInUp 0.6s ease-out forwards;
  }
  
  .stats-grid > div:nth-child(1) { animation-delay: 0.1s; }
  .stats-grid > div:nth-child(2) { animation-delay: 0.2s; }
  .stats-grid > div:nth-child(3) { animation-delay: 0.3s; }
  .stats-grid > div:nth-child(4) { animation-delay: 0.4s; }
</style>
